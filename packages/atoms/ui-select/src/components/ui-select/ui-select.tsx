import { Component, Host, h, Prop } from '@stencil/core';

@Component({
  tag: 'ui-select',
  styleUrl: 'ui-select.scss',
  shadow: true,
})
export class UiSelect {
  render() {
    return (
      <Host>
      
      </Host>
    );
  }
}
